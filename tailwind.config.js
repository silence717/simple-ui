const colors = require('./tailwind-color');

module.exports = {
  mode: 'jit',
  purge: [],
  darkMode: false,
  theme: {
    colors: colors,
  },
  variants: {
    extend: {},
  },
  plugins: [],
};
